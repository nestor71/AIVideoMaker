# ========================================
# CONFIGURAZIONE AIVIDEMAKER
# ========================================
# SETUP RAPIDO:
# 1. Copia questo file: cp .env.example .env
# 2. Genera una SECRET_KEY sicura: python -c "import secrets; print(secrets.token_urlsafe(48))"
# 3. Sostituisci SECRET_KEY nel file .env con la chiave generata
# 4. (Opzionale) Configura API keys per funzionalità avanzate

# ==================== SECURITY (OBBLIGATORIO) ====================
# IMPORTANTE: Genera secret key random (48+ caratteri) per production
# Comando: python -c "import secrets; print(secrets.token_urlsafe(48))"
# Per development puoi usare il valore di esempio sotto (NON usare in production!)
SECRET_KEY=DEVELOPMENT-ONLY-KEY-CHANGE-IN-PRODUCTION-yb8Uzk9jKunvEikzrh5HQll107e_ViasyKSWo6gUpwv

# ==================== GOOGLE OAUTH 2.0 (LOGIN CON GOOGLE) ====================
# SETUP GOOGLE OAUTH (OPZIONALE - per login con Google):
# 1. Vai a https://console.cloud.google.com/
# 2. Crea progetto o seleziona esistente
# 3. Abilita "Google+ API" o "People API"
# 4. Vai a "Credenziali" → "Crea credenziali" → "ID client OAuth 2.0"
# 5. Tipo applicazione: "Applicazione web"
# 6. URI di reindirizzamento autorizzati: http://localhost:8000/api/v1/auth/google/callback
#    (in produzione usa il tuo dominio: https://tuodominio.com/api/v1/auth/google/callback)
# 7. Copia Client ID e Client Secret qui sotto

GOOGLE_CLIENT_ID=your-google-client-id.apps.googleusercontent.com
GOOGLE_CLIENT_SECRET=your-google-client-secret
GOOGLE_REDIRECT_URI=http://localhost:8000/api/v1/auth/google/callback

# ==================== EXTERNAL API KEYS ====================

# OpenAI API Key (richiesta per generazione miniature AI e SEO metadata)
# Ottieni la tua chiave su: https://platform.openai.com/api-keys
OPENAI_API_KEY=sk-your-openai-api-key-here

# ElevenLabs API Key (richiesta per dubbing video con lip-sync)
# Ottieni la tua chiave su: https://elevenlabs.io/app/settings/api-keys
ELEVENLABS_API_KEY=your-elevenlabs-api-key-here

# ==================== DATABASE (OBBLIGATORIO) ====================
# Default: SQLite (nessun setup richiesto, perfetto per development)
DATABASE_URL=sqlite:///./aivideomaker.db

# Per PostgreSQL (production o development avanzato):
# DATABASE_URL=postgresql://aivideomaker:password@localhost:5432/aivideomaker

# ==================== ENVIRONMENT ====================
ENVIRONMENT=development
DEBUG=True

# ==================== CORS (CROSS-ORIGIN RESOURCE SHARING) ====================
# Lista domini autorizzati per richieste cross-origin (separati da virgola)
# DEVELOPMENT: http://localhost:8000,http://localhost:3000
# PRODUCTION: https://tuodominio.com,https://www.tuodominio.com,https://app.tuodominio.com
# IMPORTANTE: NON usare "*" in produzione! Specifica sempre i domini esatti.
# CORS_ORIGINS=http://localhost:8000,http://localhost:3000

# ========================================
# NOTE:
# ========================================
# ✅ CONFIGURAZIONE PRONTA ALL'USO per development:
#    - SECRET_KEY: configurata (genera una nuova per production!)
#    - DATABASE_URL: SQLite (nessun setup richiesto)
#    - ENVIRONMENT: development con DEBUG attivo
#
# 🔧 OPZIONALI (funzionalità avanzate):
#    - Google OAuth: per login con Google
#    - OpenAI: per generazione AI miniature e SEO metadata
#    - ElevenLabs: per dubbing video con lip-sync professionale
#
# ⚠️  SICUREZZA:
#    - Genera SECRET_KEY diversa per ogni environment (dev/staging/prod)
#    - Mantieni le API keys PRIVATE
#    - Il file .env è in .gitignore (non viene committato)
#    - Per production usa .env.production.template

# FastAPI Web Framework
fastapi==0.116.1
uvicorn[standard]==0.35.0
httpx==0.25.2  # HTTP client per Google OAuth

# File Upload Support
python-multipart==0.0.20

# Template Engine
jinja2==3.1.6

# Database & ORM
sqlalchemy==2.0.23

# Authentication & Security
passlib[bcrypt]==1.7.4
python-jose[cryptography]==3.3.0
argon2-cffi==23.1.0

# Configuration Management
pydantic-settings==2.0.3
python-dotenv==1.0.0

# Computer Vision
opencv-python==4.12.0.88
numpy>=2.0,<2.3  # Compatibile con opencv-python 4.12.x

# Validation
pydantic==2.9.2
email-validator==2.1.0

# File type detection
python-magic==0.4.27

# Image processing (per metadata e thumbnail generation)
Pillow==10.4.0

# OpenAI for AI thumbnail and metadata generation
openai==1.54.4
requests==2.32.3

# Whisper for audio transcription (metadata generation)
openai-whisper==20231117

# Screen recording
pyautogui==0.9.54
PyGetWindow==0.0.9

# Video Translation Dependencies
# Google Translate API (traduzione testo)
deep-translator==1.11.4

# Text-to-Speech (sintesi vocale)
gTTS==2.5.3

# YouTube Upload API
google-api-python-client
google-auth-httplib2
google-auth-oauthlib

# Video Download (YouTube, TikTok, etc.)
yt-dlp==2024.11.4

# Optional: Lip-sync (richiede GPU NVIDIA con CUDA per performance accettabili)
# Nota: Wav2Lip non è su PyPI, va installato manualmente da GitHub se necessario
# torch>=2.0.0  # Per lip-sync con Wav2Lip (molto pesante, ~3GB)
# torchvision>=0.15.0
